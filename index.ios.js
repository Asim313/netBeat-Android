import { AppRegistry } from 'react-native';
import App from './navigation/iosNavigation';

AppRegistry.registerComponent('Vaxune', () => App);

// The below line is necessary for use with the TestBed App
AppRegistry.registerComponent('Vaxune', () => App);
